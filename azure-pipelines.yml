trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'testRegPush'
    repository: 'fjdwinfratestreg'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'